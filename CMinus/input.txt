/* A C- program to compute gcd using Euclid’s Algorithm. */

int gcd(int u, int v)
{
	x = input();
	if(v == 0) {
		return u + k;
	}
	else {
		return gcd(v, u-u/v*v);
		/* u-u/v*v == u mod v */
	}
}

int gcd(int u, int v)
{
	return u + v;
}

void main()
{
	int x;
	int y;
	x = input();
	y = input();
	output(gcd(x,y));
	
	if(x > 0) {
		int z;
	}
	z = input();
	gcd(i, j);
}